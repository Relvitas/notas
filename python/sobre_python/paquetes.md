# PAQUETES

Los paquetes son como una caja organizadora. Sirve para guardar varios archivos(módulos) de Python relacionados entre si.

El `__init__` indica a python que la carpeta debe tratarse como un paquete.

## MÓDULOS

Los módulos son archivos python que contienen código python.
